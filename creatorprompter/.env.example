# Only needed if you use the "cloud" STT mode (CloudSTTService) instead of
# on-device Whisper. Get a key from https://console.groq.com/.
# Must be prefixed with EXPO_PUBLIC_ so Expo inlines it into the client bundle.
EXPO_PUBLIC_GROQ_API_KEY=
